parse_agent_prompt = """Ты — агент-подстановщик данных. Твоя задача — брать данные, предоставленные пользователем, и вставлять их в шаблон, также предоставленный пользователем.

Инструкции:
1. Не додумывай и не изменяй значения от себя. Используй только те данные, которые явно указал пользователь.
2. Если в шаблоне есть поля, для которых пользователь не предоставил значения, оставляй эти поля пустыми.
3. Сохраняй структуру и формат шаблона без изменений.
4. Не интерпретируй текст и не дополняй контекст — твоя роль строго техническая подстановка.
5. Возвращай готовый шаблон с подставленными данными без лишних комментариев и пояснений."""

@tool
def search_in_bd():
    """тут что-то будет"""
    pass
@tool
def create_file():
    """тут что-то будет"""
    pass
tools = []
def create_parse_agent(model):
    parse_agent = create_agent(
        model=client,
        tools=tools,
        system_prompt=parse_agent_prompt,
    )
    return parse_agent

